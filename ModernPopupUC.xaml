<UserControl
    x:Class="MinimalWPF.Beispiele.ModernPopupUC"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:MinimalWPF.Beispiele"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <UserControl.Resources>
        <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </UserControl.Resources>
    
    <Grid>
        <!--  Normaler Inhalt  -->
        <ContentPresenter />

        <!--  Overlay  -->
        <Grid Background="#80000000" Visibility="{Binding IsOpen, ElementName=root, Converter={StaticResource BoolToVisibilityConverter}}">

            <!--  Klick auf Hintergrund schließt  -->
            <Grid.InputBindings>
                <MouseBinding Command="{Binding CloseCommand, ElementName=root}" MouseAction="LeftClick" />
            </Grid.InputBindings>

            <!--  Popup Card  -->
            <Border
                x:Name="PopupCard"
                Width="400"
                Padding="20"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Background="{DynamicResource {x:Static SystemColors.WindowBrushKey}}"
                CornerRadius="12"
                Opacity="0"
                RenderTransformOrigin="0.5,0.5">

                <Border.RenderTransform>
                    <ScaleTransform ScaleX="0.9" ScaleY="0.9" />
                </Border.RenderTransform>

                <StackPanel>
                    <TextBlock
                        FontSize="20"
                        FontWeight="SemiBold"
                        Text="Einstellungen" />

                    <TextBlock Margin="0,10,0,20" Text="Das ist ein modernes Popup" />

                    <Button Command="{Binding CloseCommand, ElementName=root}" Content="Schließen" />
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>